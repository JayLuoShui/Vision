---
description: 使用免费 MCP 搜索实时信息
agent: codex-senior
---

使用 `searxng-public` MCP 的 `search` 工具查询 `$ARGUMENTS`。

要求：
- 优先查官方文档、GitHub、npm、PyPI、厂商页面等一手来源。
- 返回结论时带来源链接。
- 默认不要开启 detailed 搜索；只有用户要求全面调研时再用。
- 如果公共 SearXNG 源不可用，直接说明，不要编造结果。
