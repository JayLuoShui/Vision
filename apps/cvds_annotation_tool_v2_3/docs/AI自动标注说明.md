# AI 自动标注说明

AI 自动标注依赖 `ultralytics`、`torch`、`torchvision` 和 `.pt` 权重。

设备选择：

- 自动：CUDA 可用时使用 GPU，否则使用 CPU。
- CPU：始终使用 CPU。
- GPU：CUDA 不可用时弹出中文提示，不会崩溃。

批量模式：

- 跳过已有标注图片。
- 覆盖已有标注图片。
- 合并到已有标注，使用 IoU 去重。

任务执行中可以点击“取消任务”，已完成的结果会保留。
