#pragma once

#include "WcsConfig.h"

// CameraConfig, MultiCameraSystemConfig and CameraRuntimeSnapshot are defined
// in the shared WCS configuration module while the standalone application is
// being split from apps/cvds_cpp_detector.
