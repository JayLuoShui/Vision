// Transitional camera UI/manager bridge.
// CameraTileWidget remains in the shared implementation while the WCS app is
// split into its own directory. This file gives the standalone app a stable
// CameraManager.cpp compilation unit and supplies the current tile widget code.
#include "../../cvds_cpp_detector/src/CameraTileWidget.cpp"
