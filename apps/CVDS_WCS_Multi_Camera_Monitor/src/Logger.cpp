// Logger is reserved for a standalone rotating log sink in the WCS app.
// The current Qt window writes runtime text into the operator log panel.
namespace cvds_wcs {
void keepLoggerTranslationUnit() {}
}  // namespace cvds_wcs
